{
  "version": "1.0.25",
  "url": "https://streamgraphicspro.com/dl.php?u=1",
  "notes": "Fixes the licence report, which was being refused by the web host and failing silently. The app did not put a name on its outgoing requests, and a request that does not identify itself looks like a bot, so the host's firewall answered 403 to every report. Update checks and the revoked-key list were never affected - those are plain files the firewall does not see. Nothing else changes."
}
